services:
  app:
    build:
      context: .
      target: dev

    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - node_modules:/app/node_modules

    environment:
      CHOKIDAR_USEPOLLING: "true"

volumes:
  node_modules: